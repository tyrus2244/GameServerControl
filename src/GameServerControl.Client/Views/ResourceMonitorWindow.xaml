<Window x:Class="GameServerControl.Client.Views.ResourceMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resource Monitor"
        Width="800" Height="540"
        Background="{StaticResource BgBrush0}"
        Foreground="{StaticResource TextBrush}"
        TextElement.Foreground="{StaticResource TextBrush}"
        TextElement.FontFamily="Segoe UI"
        TextElement.FontSize="13"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="HeaderText" Text="Resource Monitor" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Live CPU and RAM sampled every 2 seconds. History keeps the last 5 minutes."
                       Foreground="{StaticResource TextDimBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- CPU chart -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="CPU" FontWeight="SemiBold" Foreground="{StaticResource InfoBrush}"/>
                    <TextBlock x:Name="CpuLabel" Text="—" Margin="14,0,0,0" Foreground="{StaticResource TextDimBrush}"/>
                </StackPanel>
                <Canvas Grid.Row="1" x:Name="CpuCanvas" Background="{StaticResource BgBrush2}" ClipToBounds="True">
                    <Polyline x:Name="CpuLine" Stroke="#FF6B3D" StrokeThickness="2" Points=""/>
                </Canvas>
            </Grid>
        </Border>

        <!-- RAM chart -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="RAM" FontWeight="SemiBold" Foreground="{StaticResource InfoBrush}"/>
                    <TextBlock x:Name="RamLabel" Text="—" Margin="14,0,0,0" Foreground="{StaticResource TextDimBrush}"/>
                </StackPanel>
                <Canvas Grid.Row="2" x:Name="RamCanvas" Background="{StaticResource BgBrush2}" ClipToBounds="True">
                    <Polyline x:Name="RamLine" Stroke="#DC1E2D" StrokeThickness="2" Points=""/>
                </Canvas>
            </Grid>
        </Border>

        <TextBlock Grid.Row="3" x:Name="StatusText" Margin="0,12,0,0" Foreground="{StaticResource TextDimBrush}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Close" Style="{StaticResource ActionButton}" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
